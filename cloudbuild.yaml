steps:
# Install node package
- name: 'gcr.io/cloud-builders/npm'
  args: [ 'install' ]

# Build files
- name: 'gcr.io/cloud-builders/npm'
  args: [ 'run', 'build', '--prod' ]

# Deploy to google cloud app engine.
- name: 'gcr.io/cloud-builders/npm'
  args: [ 'install' ]
